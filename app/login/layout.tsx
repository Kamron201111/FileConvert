import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Kirish — FileConvert.uz',
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
